% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ols4_mcp.R
\name{ols4_chat}
\alias{ols4_chat}
\title{Create an ellmer chat pre-wired with EBI OLS4 MCP tools}
\usage{
ols4_chat(
  provider = "anthropic",
  model = "claude-sonnet-4-5",
  temperature = 0,
  url = "https://www.ebi.ac.uk/ols4/api/mcp",
  ...
)
}
\arguments{
\item{provider}{character(1) LLM provider; see \code{\link{llm_env_var}}.
Defaults to \code{"anthropic"}.}

\item{model}{character(1) model identifier. Defaults to
\code{"claude-sonnet-4-5"}.}

\item{temperature}{numeric(1) sampling temperature passed to the LLM.
Defaults to \code{0} for deterministic, reproducible output.}

\item{url}{character(1) OLS4 MCP endpoint URL passed to
\code{\link{ols4_mcp_tools}}.}

\item{\dots}{additional arguments passed to \code{\link{llm_chat}}.}
}
\value{
an ellmer \code{Chat} object with all OLS4 tools registered.
}
\description{
Calls \code{\link{ols4_mcp_tools}} to retrieve the 12 OLS4 search tools,
creates an ellmer chat object via \code{\link{llm_chat}}, and registers all
tools on the chat so the LLM can invoke them during structured calls.
}
\note{
Starting the \code{mcp-remote} bridge takes a few seconds.
Reuse the returned object across multiple calls rather than creating
a new one each time.
}
\examples{
if (interactive()) {
    ch <- ols4_chat()
    map_concepts("chromatin accessibility and histone modification", chat = ch)
}
}
