% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vig2data.R
\name{vig2data}
\alias{vig2data}
\title{Use the extract_data facility defined in ellmer's doc to obtain summary information about textual content.
Originally tailored to vignettes in bioconductor; it is newly generalized to handle any pdf, html or text in URL.}
\usage{
vig2data(
  url = "https://bioconductor.org/packages/release/bioc/html/Voyager.html",
  maxnchar = 30000,
  n_pdf_pages = 10,
  model = "gpt-4o",
  provider = "openai",
  ...
)
}
\arguments{
\item{url}{character(1) URL for an html bioconductor vignettes}

\item{maxnchar}{numeric(1) text is truncated to a substring with this length}

\item{n_pdf_pages}{numeric(1) maximum number of pages to extract text from for pdf vignettes}

\item{model}{character(1) model identifier for the selected provider; defaults to "gpt-4o" (OpenAI)}

\item{provider}{character(1) LLM provider; see \code{\link{llm_env_var}} for supported values and
the required environment variable for each.  Defaults to "openai".}

\item{\dots}{passed to the underlying \code{chat_*} function via \code{\link{llm_chat}}}
}
\value{
a list with components author, topics, focused, coherence, and persuasion
}
\description{
Use the extract_data facility defined in ellmer's doc to obtain summary information about textual content.
Originally tailored to vignettes in bioconductor; it is newly generalized to handle any pdf, html or text in URL.
}
\note{
Based on code from https://cran.r-project.org/web/packages/ellmer/vignettes/structured-data.html
March 15 2025.  The API key for the chosen provider must be available in the corresponding
environment variable (e.g. OPENAI_API_KEY for "openai", ANTHROPIC_API_KEY for "anthropic").
}
\examples{
if (interactive()) {
# OPENAI_API_KEY must be set for the default provider
tst = vig2data()
str(tst)
}
}
